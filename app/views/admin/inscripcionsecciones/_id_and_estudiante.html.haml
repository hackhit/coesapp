%td= "#{i+1} (#{es.id})"
%td
	= "(#{es.estudiante_id})"
	- if current_admin
		= link_to es.estudiante.usuario do
			= raw es.nombre_estudiante_con_retiro_plus
	- else
		= raw es.nombre_estudiante_con_retiro_plus
