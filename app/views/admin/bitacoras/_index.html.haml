.border.mt-3.p-3
  %table.dataTable.table.table-striped.table-sm.table-hover.table-bordered
    %thead
      %tr
        %th Id
        %th Fecha
        %th Usuario
        %th Objeto
        %th Tipo Trans
        %th Descripción

    %tbody

      - @bitacoras.each do |bita| 
        %tr
          %td= bita.id
          %td= "Hace #{distance_of_time_in_words(Time.now-4.hours, bita.created_at-4.hours)} (#{(bita.created_at-4.hours).strftime('%d/%m/%Y %H:%M %p')})"
          %td= bita.usuario.descripcion if bita.usuario
          %td= bita.tipo_objeto
          %td= bita.tipo.capitalize if bita.tipo
          %td= bita.descripcion
