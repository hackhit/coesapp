.border.mt-3.p-3
  %table.table.table-striped.table-sm.table-hover.table-bordered
    %thead
      %tr
        %th Fecha
        %th Usuario
        %th Objeto
        %th Tipo Trans
        %th Descripción
        %th IP Origen

    %tbody

      - @bitacoras.each do |bita| 
        %tr.align-middle
          %td= "Hace #{distance_of_time_in_words(Time.now, bita.created_at)} (#{l bita.created_at, format: "%d/%m/%Y - %I:%M%p"})"
          %td= bita.usuario.descripcion if bita.usuario
          %td= bita.tipo_objeto
          %td= bita.tipo
          %td= bita.descripcion
          %td= bita.ip_origen