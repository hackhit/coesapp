%p#notice= notice

%p
  %b Contenido:
  = raw @comentario.contenido
%p
  %b Estado:
  = @comentario.estado.capitalize

= link_to 'Editar', edit_comentario_path(@comentario), class: 'btn btn-sm btn-primary mr-3'

= link_to 'Regresar', comentarios_path, class: 'btn btn-sm btn-outline-secondary'
